#include <unistd.h>
void ft_is_negative(int n)
{
    if (n < 0)
    {
        write(1, "n", 1);
        
    } else {
        write(1, "p", 1);
    }
}
int main()
{
    int number= 1;
    ft_is_negative(number);
    return 0;
}
